function createCounter(init)
{
    let val = init

    return {
        increment:function()
        {
            return ++val
        },
        decrement:function()
        {
            return --val
        },
        reset:function()
        {
            return val = init
        }
    }
}
